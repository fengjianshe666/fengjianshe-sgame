# 坦克大战游戏优化计划

## Context
对单文件 HTML 坦克大战游戏进行全面优化，使其更加流畅、高级，但**不改变任何原有功能**。优化涵盖性能、视觉效果、游戏手感三个方面。

---

## 优化项（共 8 大项）

### 1. 屏幕震动系统
- 新增全局变量 `shakeIntensity`, `shakeDuration`
- `draw()` 中检测震动状态，用 `ctx.translate` 偏移画面
- `update()` 中震动逐渐衰减
- 触发点：玩家受伤、基地受伤、大爆炸、BOSS 大招、大招释放
- **移除旧的 CSS transform 震动 hack**

### 2. 粒子系统优化
- 粒子数量上限 `MAX_PARTICLES = 200`，防止无限增长
- 子弹拖尾粒子从每帧创建改为每 0.05s 创建一次
- 将 `array.filter()` 清理改为原地 compact 模式（5 个数组）避免新数组分配
- 粒子渲染去除 `shadowBlur` 改用简单 `arc` 提高性能

### 3. 命中闪烁效果
- `Tank` 类增加 `flashTimer` 属性
- 被击中时 `flashTimer = 0.08`，绘制时叠加白色闪光层
- 适用于敌人和玩家

### 4. HUD 平滑动画
- 分数变化时数字弹跳缩放效果
- 大招就绪时脉冲呼吸动画
- 纯 CSS + DOM 操作实现

### 5. 爆炸视觉增强
- 新增 `shockRings` 冲击波环（大爆炸时向外扩散的圆环）
- 大爆炸（count>=20）增加白色火花粒子
- 新增 `screenFlash` 全屏白色闪光
- 触发点：大爆炸、玩家受伤

### 6. 核心性能优化
- `forEach` → `for` 循环（5 处）
- 碰撞检测中用 AABB 快速剔除替代直接 `Math.hypot` 计算
- 大招循环中预计算角度步长
- 碰撞检测中 `splice` 改为标记+compact 模式

### 7. 伤害数字弹出
- 新增 `damageNumbers` 数组
- 敌人/玩家/基地受伤时弹出浮动数字
- 向上飘动并逐渐透明消失
- 上限 20 个

### 8. 额外细节
- 网格随玩家位置微移（视差效果）
- 敌人命中时生成命中标记环
- BOSS 登场炫酷特效
- 游戏重置时清理所有新增状态

---

## 修改范围
- 仅修改 `g:\AI专项\ai软件\坦克大战\index.html`
- 涉及：全局变量声明、Particle 类、Bullet 类、Tank 类、Player 类、Enemy/Boss 类、`checkCollisions`、`update`、`draw`、`updateHUD`、`startGame`/`startDemo`/`exitDemo`、CSS 样式

## 验证方法
刷新页面后：
1. 点击"光束演示"确认光束效果正常
2. 点击"启动引擎"正常游玩，观察：
   - 被击中时有屏幕震动 + 白色闪光 + 伤害数字
   - 击杀敌人时爆炸有冲击波环
   - 分数变化时 HUD 数字弹跳
   - 大招就绪时状态指示器脉冲
   - 游戏流畅度提升（帧率稳定）
3. 战败时确认 CG 全屏展示正常
4. 背景图正常显示