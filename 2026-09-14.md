# 2026-09-14 工作日志

## 网站背景改造：Uiverse jp-matrix 数字矩阵
- 将 `index.html` 破冰 Bingo 游戏的浅色渐变背景改为 Uiverse.io (rikku94-lab) 的 `.jp-matrix` 深色数字矩阵背景。
- 改动点：
  1. `body` 背景改为 `#05050a`。
  2. 在 `<style>` 注入完整 `.jp-matrix` 样式 + `smooth-pulse` 关键帧，并加 `position:fixed; top:0; left:0; z-index:0; pointer-events:none` 使其作为固定背景层。
  3. `body` 内、`#app` 前插入 `<div class="jp-matrix" id="matrix-bg">`；`#app` 加 `z-10` 置于背景之上。
  4. 新增 IIFE 脚本：按视口大小（≥48列×27行）生成片假名+数字字符 `<span>` 填充矩阵，resize 时防抖重排。
- 前台原有浅色玻璃卡片在深色矩阵上仍清晰可读，无需改前景配色。

## 九宫格特质改为每次打开随机刷新
- 需求：每个用户/每次点开都出现不同的 9 宫格内容。
- 改动 `generateGrid()`：原先洗牌 19 位同学取前 9 人各映射 1 个爱好；改为汇总全体同学爱好构建「去重特质池」，Fisher-Yates 洗牌后抽取 9 个互不重复的随机特质（每个特质仍保证有对应同学可匹配）。
- 改动 `initGame()`：删除从 localStorage 恢复存档的逻辑，改为每次打开都重新 `generateGrid()` 生成全新随机九宫格（计时器重置）。因此重复打开/不同设备看到的格子内容都不一致。
- 注意：此改动牺牲了「中途刷新进度续接」能力，符合用户「每次点开不一致」的诉求。

## 每格限填 3 次 + 锁死 + 首页规则提醒
- 需求：每格只能填 3 次，三次全错永久锁死不可点亮；首页加游戏规则提醒。
- `generateGrid()` 每个格子新增 `attempts:0`、`locked:false` 字段。
- `selectStudent()`：填错（同学无该特质）时 `attempts+1`；满 3 次置 `locked=true` 并提示「已锁死 🔒」、关闭弹窗；未满则提示剩余次数。填错不关闭弹窗，可继续重试。
- `openSelectModal()`：点击已锁死格子直接 toast 拦截，不打开弹窗。
- 格子模板：锁死格显示灰色虚线 + 删除线特质 + 🔒「已锁死」；未匹配格底部显示「可试 X 次」。
- 新增 `.bingo-cell-locked` 样式。
- 顶栏加「规则」信息按钮；下方新增可关闭的规则提醒横幅（列出 3 次锁死规则），关闭后写 localStorage `bingo_rules_dismissed` 不再自动弹出，可随时点规则按钮重新打开。
- setup 暴露 `showRules` / `dismissRules`。

## 通关动画：旋转星辰 → 大大的 BINGO
- 需求：连成 3 连线（横竖斜）时全屏播放 Uiverse 星辰动画，最后出现大大的 BINGO 图案。
- 将用户提供的 BINGO 透明 PNG 复制为项目内 `bingo.png`。
- 注入 Uiverse (narmesh_sah) `.preloader` / `.crack`~`.crack5` 星辰样式 + `rotate` 关键帧；星辰容器包在 `.fanfare-burst`（min(72vw,360px)）内，`.is-done` 时放大淡出。
- 新增 `.bingo-logo` + `bingo-pop` 关键帧：BINGO 图从 0.2 缩放旋转弹入。
- 新增全屏叠加层 `showBingoFanfare`（z-[80]，点击任意处提前进入凭证）。
- `triggerBingoEffect()` 重写：先留 650ms 让连线高亮，再星辰登场(650ms)，1.6s 后星辰淡出弹出 BINGO，BEAT+4.6s 自动 `finishFanfare()` → 展示领奖凭证；礼花持续 2.5s。
- 新增 `finishFanfare()`、`fanfareTimers` 定时器数组（点击提前结束时 clearTimeout）；onUnmounted 清理。
- setup 暴露 `showBingoFanfare` / `showBingoLogo` / `finishFanfare`。
